import styled from "styled-components";

const Landing = () => {
  return <Container>아아아안녕하세요</Container>;
};

export default Landing;

const Container = styled.div`
  width: 100vw;
  overflow: hidden;
`;
